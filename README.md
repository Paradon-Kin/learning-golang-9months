## 📅 Learning Log (บันทึกการเรียนรู้)

| Day | Date | Topic (หัวข้อที่เรียน) | Project (ผลงานที่ทำ) | Status | Key Takeaways (สิ่งที่ได้เรียนรู้) |
| :---: | :---: | :--- | :--- | :---: | :--- |
| **01** | 2026-01-02 | Introduction to Go, Variables, Input/Output | 1. BMI Calculator<br>2. Ideal Weight Challenge | ✅ | เรียนรู้โครงสร้าง Go, `var`, `func main`, การรับค่า `fmt.Scan` และการคำนวณเบื้องต้น |
| **02** | 2026-01-03 | Data Types, Constants, Functions & Switch-Case | Currency Converter (ร้านแลกเงิน)<br>*(Features: Bidirectional, Fee, Menu)* | ✅ | 1. **Data Types:** ความต่างของ `int` vs `float64` และการแปลง Type<br>2. **Clean Code:** ใช้ `const` แทน Magic Numbers และแยก `func` เพื่อความเป็นระเบียบ<br>3. **Logic:** การใช้ `switch-case` ทำเมนู และการคำนวณหักค่าธรรมเนียม (Business Logic)<br>4. **UX:** การจัด Format ตัวเลข (`%.2f`) และเว้นบรรทัดให้ใช้งานง่าย |
| **03** | 2026-01-04 | Loops & Control Flow | Multiplication Table Generator<br>(เครื่องสร้างตารางสูตรคูณ) | ✅ | 1. **Loops:** การใช้ `for` ในรูปแบบต่างๆ (Classic, While-style)<br>2. **Flow Control:** การใช้ `break` เพื่อออกจากลูป และ `continue` เพื่อข้ามรอบ<br>3. **Nested Loops:** การวนลูปซ้อนลูปเพื่อสร้างแพทเทิร์นตาราง |
| **04** | 2026-01-06 | Arrays & Slices | Donghua List Manager V1.0<br>*(ระบบจัดการรายชื่ออนิเมะ)* | ✅ | 1. **Slices:** ความยืดหยุ่นของ Slice ที่เหนือกว่า Array (ขนาดไม่คงที่)<br>2. **Slice Ops:** การใช้ `append` เพิ่มข้อมูล และเทคนิค Slicing (`[:i]...`) เพื่อลบข้อมูล<br>3. **Iteration:** การใช้ `range` เพื่อวนลูปดึงค่า Index และ Value ออกมาใช้งาน |
| **05** | 2026-01-08 | Structs, Auto-ID & POS Logic | **Mini Cafe Manager**<br>*(ระบบจัดการร้านกาแฟ & คิดเงิน)* | ✅ | 1. **Structs:** การออกแบบ Custom Type (`type Drink`) เพื่อเก็บข้อมูลสินค้าที่มีหลายคุณสมบัติ<br>2. **Auto-ID Algorithm:** ลอจิกการสร้าง ID อัตโนมัติโดยอิงจาก ID ตัวสุดท้าย (`len-1`) + 1<br>3. **CRUD Mastery:** การประยุกต์ใช้ Create, Read, Update (ผ่าน Index), Delete ครบวงจร<br>4. **Cashier Logic:** การคำนวณเงิน (`Price * Qty`), เงินทอน, และ Validation ป้องกันเงินติดลบ/จ่ายไม่พอ |